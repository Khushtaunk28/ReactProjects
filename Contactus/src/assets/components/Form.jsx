import { form } from 'framer-motion/client'
import React from 'react'

const Form = () => {
  return (
    <form action="contactfrom">
        <button>VIA SUPPORT CHAT</button>
        <button>VIA CALL</button>
    </form>
  )
}

export default Form